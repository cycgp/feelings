type AttendanceInfo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  phone: String!
  email: AWSEmail!
  relationship: Int!
  guestCount: Int!
  vegetarion: Boolean!
}

type Message @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  email: String!
  content: String!
}
